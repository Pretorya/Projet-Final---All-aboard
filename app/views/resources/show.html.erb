<% content_for :title, @resource.title %>

<div class="max-w-3xl mx-auto py-8 px-4">
  <%= link_to resources_path, class: "text-gray-400 hover:text-white transition-colors text-sm inline-flex items-center gap-2 mb-6" do %>
    <i class="fas fa-arrow-left"></i> Toutes les ressources
  <% end %>

  <div class="glass rounded-2xl p-8">
    <div class="flex items-start justify-between gap-4 mb-6">
      <h1 class="text-2xl font-bold"><%= @resource.title %></h1>
      <div class="flex items-center gap-2 shrink-0">
        <% if @resource.subject %>
          <span class="px-3 py-1 bg-white/10 rounded-full text-sm"><%= @resource.subject.name %></span>
        <% end %>
        <% if current_user == @resource.user || current_user.admin? %>
          <%= link_to edit_resource_path(@resource), class: "p-2 rounded-lg bg-white/5 hover:bg-white/10 text-gray-400 hover:text-gray-200 transition-colors", title: "Modifier" do %>
            <i class="fas fa-pen text-sm"></i>
          <% end %>
          <%= button_to resource_path(@resource), method: :delete,
              data: { turbo_confirm: "Supprimer cette ressource définitivement ?" },
              class: "p-2 rounded-lg bg-red-500/10 hover:bg-red-500/20 text-red-400 transition-colors",
              title: "Supprimer" do %>
            <i class="fas fa-trash text-sm"></i>
          <% end %>
        <% end %>
      </div>
    </div>

    <div class="flex items-center gap-3 mb-6 pb-6 border-b border-white/10">
      <img src="<%= @resource.user.profile_avatar %>" class="w-8 h-8 rounded-full">
      <div>
        <p class="text-sm font-medium">
          <%= @resource.user.display_name %>
          <span class="ml-2 text-xs text-emerald-400 font-semibold">
            <i class="fas fa-chalkboard-teacher mr-1"></i>Mentor
          </span>
        </p>
        <p class="text-xs text-gray-500">Publié <%= time_ago_in_words(@resource.created_at) %></p>
      </div>
    </div>

    <div class="text-gray-200 leading-relaxed whitespace-pre-wrap">
      <%= @resource.body %>
    </div>
  </div>
</div>
