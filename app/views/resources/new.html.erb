<% content_for :title, "Nouvelle ressource" %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <%= link_to mentor_dashboard_path, class: "text-gray-400 hover:text-white transition-colors text-sm inline-flex items-center gap-2 mb-6" do %>
    <i class="fas fa-arrow-left"></i> Mon espace Mentor
  <% end %>

  <div class="glass rounded-2xl p-8">
    <h1 class="text-2xl font-bold mb-6">
      <i class="fas fa-plus-circle text-emerald-400 mr-2"></i>Nouvelle ressource
    </h1>

    <%= render "form", resource: @resource %>
  </div>
</div>
