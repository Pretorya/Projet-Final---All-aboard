<footer class="border-t border-white/5 mt-12 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col md:flex-row items-center justify-between gap-4">
      <div class="flex items-center gap-2 text-gray-500 text-sm">
        <i class="fas fa-graduation-cap text-primary"></i>
        <span class="gradient-text font-semibold">AllAboard</span>
        <span>&copy; <%= Date.current.year %> — Tous droits réservés</span>
      </div>
      <nav class="flex flex-wrap justify-center gap-x-6 gap-y-2 text-sm text-gray-500">
        <%= link_to "CGU", legal_cgu_path, class: "hover:text-white transition-colors" %>
        <%= link_to "Confidentialité", legal_privacy_path, class: "hover:text-white transition-colors" %>
        <%= link_to "Mentions légales", legal_mentions_path, class: "hover:text-white transition-colors" %>
      </nav>
    </div>
  </div>
</footer>
